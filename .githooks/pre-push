#!/usr/bin/env bash
# pre-push — never push red. Run the same gate CI runs before anything leaves
# the machine. Emergency bypass: git push --no-verify
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"

[[ -x scripts/check.sh ]] || exit 0
echo "pre-push: scripts/check.sh"
if ! ./scripts/check.sh; then
    echo "✗ Gate failed — push aborted. Fix it, or bypass: git push --no-verify" >&2
    exit 1
fi
